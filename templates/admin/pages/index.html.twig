{% extends 'admin.html.twig' %}

{% block body %}
    <section class="content-header">
        <h1>
            Home page
        </h1>

        <ol class="breadcrumb">
            <li class="active">Home</li>
        </ol>
    </section>

    <section class="content">
        <div class="row">
            <div class="col-xs-12">
                {{ 'admin.index.welcome_text'|trans({
                        '%user%': app.user.fullname,
                        '%project%': name_admin
                    })
                }}
            </div>
        </div>
    </section>
{% endblock %}