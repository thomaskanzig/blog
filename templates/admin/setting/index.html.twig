{% extends 'admin.html.twig' %}

{% block body %}
    <section class="content-header">
        <h1>
            Setting
        </h1>
        <ol class="breadcrumb">
            <li><a href="{{ path('admin_pages_index') }}"><i class="fa fa-home"></i> Home</a></li>
            <li class="active">Setting</li>
        </ol>
    </section>

    <!-- Main content -->
    <section class="content">
        <div class="row">
            <div class="col-md-12">
                <!-- general form elements -->
                <div class="box box-primary">
                    <div class="box-header with-border">
                        <h3 class="box-title">Form</h3>
                    </div>

                    {{ form_start(settingForm) }}
                        <div class="box-body">
                            {{ form_row(settingForm.google_gtag_id) }}
                            {{ form_row(settingForm.url_facebook) }}
                            {{ form_row(settingForm.url_instagram) }}
                            {{ form_row(settingForm.url_linkedin) }}
                            {{ form_row(settingForm.url_github) }}
                        </div>

                        <div class="box-footer">
                            <button type="submit" class="btn btn-primary">Save setting</button>
                        </div>
                    {{ form_end(settingForm) }}
                </div>
                <!-- /.box -->
            </div>
        </div>
        <!-- /.row -->
    </section>
    <!-- /.content -->
{% endblock %}